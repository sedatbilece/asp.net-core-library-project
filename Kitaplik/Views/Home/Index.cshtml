@model List<Kitaplik.Models.Kitaplar>


@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    

    İNDEX SAYFASI

    <a href="/Home/StaticTema">static tem sayfası</a>

    <table class="table table-bordered table-hover table-striped" >
        
        <tr>
            <th>id</th>
            <th>kitap adı</th>
            <th>yazar</th>
        </tr>

        @foreach (var k in Model)
        {
    <tr>
        <td>@k.ID </td>
        <td>@k.KitapAd</td>
        <td>@k.Yazar</td>
    </tr>
        }

        
    </table>

    <hr />

    @Html.Partial("view")

<hr />
<h2>gelecek kitaplar</h2>


@await Component.InvokeAsync("YeniKitaplar") 

</div>
